// real life example of mail
// from ->
// data -> orignal data within the packet
// to <-
// every request & response conatins headers -> extra information about the data

// like host , content-type

// HTTP headers are an important part of the API request and response as they represent the meta-data associated with the api request and response

// headers carry information for the request and response body

// http - headers just like -> meta-deta

// set our own headers -> response.setHeaders("name","Harsh Kumar") time of reponse

// print request headers -> clg(request.headers)

// always add X to the custome header like -> X-Powered-By - Express <- in response header we get

// built in headers type in api -> check on google